# .NET
bin/
obj/
*.user
*.suo
.vs/
artifacts/
TestResults/

# Secrets locaux — jamais dans git (règle 16)
.env
*.local.json
local.settings.json

# OS / éditeurs
.DS_Store
Thumbs.db
.idea/
.vscode/

# Note : les skills d'assistance installés (via « npx skills add … ») SONT suivis dans le dépôt,
# à la demande de l'utilisateur — voir .agents/skills, .claude/skills et skills-lock.json.

# Troisième copie des mêmes skills, déposée par l'outillage a côté de .agents/skills et
# .claude/skills qui sont déjà suivis : on n'en garde qu'un exemplaire dans le dépôt.
.github/skills/
.github/hooks/