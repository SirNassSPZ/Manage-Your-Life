# .NET
bin/
obj/
*.user
*.suo
.vs/
artifacts/
TestResults/

# Secrets locaux — jamais dans git (règle 16)
.env
*.local.json
local.settings.json

# OS / éditeurs
.DS_Store
Thumbs.db
.idea/
.vscode/

# Skills d'assistance installés localement (outillage de dev, pas du code produit) —
# réinstallables via « npx skills add … » ; on ne pollue pas l'historique produit.
# Liste blanche : seuls les skills PRODUIT (garde-fous) sont suivis ; tout skill installé l'est en local.
.agents/
skills-lock.json
.claude/skills/*
!.claude/skills/garde-fou-architecture
!.claude/skills/garde-fou-synchro
