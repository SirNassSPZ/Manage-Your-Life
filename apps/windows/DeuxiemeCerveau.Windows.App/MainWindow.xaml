<Window x:Class="DeuxiemeCerveau.Windows.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DeuxiemeCerveau.Windows.App.ViewModels"
        Title="Deuxième Cerveau" Height="750" Width="1200"
        Background="{StaticResource BackgroundPrimary}"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation (Glassmorphic Dark Mode) -->
        <Border Grid.Column="0" Background="{StaticResource BackgroundSecondary}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,1,0">
            <DockPanel Margin="16,24">
                <!-- App Header -->
                <StackPanel DockPanel.Dock="Top" Margin="8,0,0,32">
                    <TextBlock Text="Deuxième Cerveau" Foreground="{StaticResource TextPrimary}" FontSize="20" FontWeight="Bold"/>
                    <TextBlock Text="Gestion Personnelle Local-First" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Navigation Menu -->
                <StackPanel DockPanel.Dock="Top">
                    <RadioButton Content="💳  Finances" Command="{Binding NaviguerFinancesCommand}" IsChecked="True" Style="{StaticResource {x:Type ToggleButton}}" Height="44" Margin="0,4" Padding="12,0" Foreground="{StaticResource TextPrimary}" Background="Transparent" FontSize="14"/>
                    <RadioButton Content="📅  Calendrier" Command="{Binding NaviguerCalendrierCommand}" Style="{StaticResource {x:Type ToggleButton}}" Height="44" Margin="0,4" Padding="12,0" Foreground="{StaticResource TextPrimary}" Background="Transparent" FontSize="14"/>
                    <RadioButton Content="📝  Note libre" Command="{Binding NaviguerNotesCommand}" Style="{StaticResource {x:Type ToggleButton}}" Height="44" Margin="0,4" Padding="12,0" Foreground="{StaticResource TextPrimary}" Background="Transparent" FontSize="14"/>
                    <RadioButton Content="🗑️  Corbeille" Command="{Binding NaviguerCorbeilleCommand}" Style="{StaticResource {x:Type ToggleButton}}" Height="44" Margin="0,4" Padding="12,0" Foreground="{StaticResource TextPrimary}" Background="Transparent" FontSize="14"/>
                    <RadioButton Content="⚙️  Réglages" Command="{Binding NaviguerReglagesCommand}" Style="{StaticResource {x:Type ToggleButton}}" Height="44" Margin="0,4" Padding="12,0" Foreground="{StaticResource TextPrimary}" Background="Transparent" FontSize="14"/>
                </StackPanel>

                <!-- Footer info -->
                <StackPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
                    <Border Style="{StaticResource GlassCard}" Padding="12">
                        <StackPanel>
                            <TextBlock Text="Statut Synchro" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold"/>
                            <TextBlock Text="🟢 Connecté &amp; Synchronisé" Foreground="{StaticResource AccentSuccess}" FontSize="12" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Content Area -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="32">
                <!-- Content Host switching depending on Current ViewModel -->
                <ContentControl Content="{Binding VueCourante}">
                    <ContentControl.Resources>
                        <!-- Finances Template -->
                        <DataTemplate DataType="{x:Type vm:FinancesViewModel}">
                            <StackPanel>
                                <TextBlock Text="Finances &amp; Solde de référence" Foreground="{StaticResource TextPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,24"/>
                                
                                <!-- Solde Card -->
                                <Border Style="{StaticResource GlassCard}" Margin="0,0,0,24">
                                    <StackPanel>
                                        <TextBlock Text="SOLDE DE RÉFÉRENCE ACTUEL" Foreground="{StaticResource TextMuted}" FontSize="12" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding SoldeEurosFormatted}" Foreground="{StaticResource AccentSuccess}" FontSize="36" FontWeight="Bold" Margin="0,8,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Saisie Nouvelle Transaction -->
                                <Border Style="{StaticResource GlassCard}" Margin="0,0,0,24">
                                    <StackPanel>
                                        <TextBlock Text="Ajouter une transaction (Facture / Paiement / Revenu)" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                        <Grid Margin="0,0,0,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="150"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox Text="{Binding NouveauTitre, UpdateSourceTrigger=PropertyChanged}" Background="{StaticResource BackgroundSecondary}" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderSubtle}" Padding="10" FontSize="14" Grid.Column="0" Margin="0,0,12,0"/>
                                            <TextBox Text="{Binding NouveauMontantEuros, UpdateSourceTrigger=PropertyChanged}" Background="{StaticResource BackgroundSecondary}" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderSubtle}" Padding="10" FontSize="14" Grid.Column="1"/>
                                        </Grid>
                                        <Button Content="Ajouter la transaction (Local-First)" Command="{Binding AjouterTransactionCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </Border>

                                <!-- Liste des Transactions -->
                                <TextBlock Text="Historique des transactions" Foreground="{StaticResource TextPrimary}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                <ItemsControl ItemsSource="{Binding Transactions}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GlassCard}" Margin="0,4" Padding="12">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding Titre}" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" FontSize="14"/>
                                                        <TextBlock Text="{Binding Type}" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="1" Text="{Binding MontantCentimes}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>

                        <!-- Calendrier Template -->
                        <DataTemplate DataType="{x:Type vm:CalendrierViewModel}">
                            <StackPanel>
                                <TextBlock Text="Calendrier unifié (Rendez-vous + Échéances)" Foreground="{StaticResource TextPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,24"/>
                                
                                <!-- Ajouter Rendez-vous -->
                                <Border Style="{StaticResource GlassCard}" Margin="0,0,0,24">
                                    <StackPanel>
                                        <TextBlock Text="Nouveau rendez-vous" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                        <TextBox Text="{Binding NouveauRendezvousTitre, UpdateSourceTrigger=PropertyChanged}" Background="{StaticResource BackgroundSecondary}" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderSubtle}" Padding="10" FontSize="14" Margin="0,0,0,12"/>
                                        <Button Content="Planifier le rendez-vous" Command="{Binding AjouterRendezvousCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </Border>

                                <ItemsControl ItemsSource="{Binding Evenements}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GlassCard}" Margin="0,4" Padding="12">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Titre}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" FontSize="14"/>
                                                    <TextBlock Text="{Binding Type}" Foreground="{StaticResource AccentSecondary}" FontSize="12"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>

                        <!-- Notes Template -->
                        <DataTemplate DataType="{x:Type vm:NotesViewModel}">
                            <StackPanel>
                                <TextBlock Text="Note libre (Brouillon texte)" Foreground="{StaticResource TextPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,24"/>
                                <Border Style="{StaticResource GlassCard}" Margin="0,0,0,24">
                                    <StackPanel>
                                        <TextBox Text="{Binding NouvelleNoteTexte, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="100" Background="{StaticResource BackgroundSecondary}" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderSubtle}" Padding="10" FontSize="14" Margin="0,0,0,12"/>
                                        <Button Content="Enregistrer la note" Command="{Binding AjouterNoteCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </Border>
                                <ItemsControl ItemsSource="{Binding Notes}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GlassCard}" Margin="0,4" Padding="12">
                                                <TextBlock Text="{Binding Description}" Foreground="{StaticResource TextPrimary}" FontSize="14"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>

                        <!-- Corbeille Template -->
                        <DataTemplate DataType="{x:Type vm:CorbeilleViewModel}">
                            <StackPanel>
                                <TextBlock Text="Corbeille (Éléments marqués supprimés)" Foreground="{StaticResource TextPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,24"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                    <Button Content="Restaurer l'élément" Command="{Binding RestaurerCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,12,0"/>
                                    <Button Content="Purger définitivement (Manuel)" Command="{Binding PurgerCommand}" Background="{StaticResource AccentDanger}" Foreground="White" Padding="16,8" BorderThickness="0"/>
                                </StackPanel>
                                <ItemsControl ItemsSource="{Binding ElementsSupprimes}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GlassCard}" Margin="0,4" Padding="12">
                                                <Grid>
                                                    <TextBlock Text="{Binding Titre}" Foreground="{StaticResource TextSecondary}" FontSize="14" TextDecorations="Strikethrough"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>

                        <!-- Réglages Template -->
                        <DataTemplate DataType="{x:Type vm:ReglagesViewModel}">
                            <StackPanel>
                                <TextBlock Text="Réglages &amp; Exportation local-first" Foreground="{StaticResource TextPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,24"/>
                                <Border Style="{StaticResource GlassCard}" Margin="0,0,0,24">
                                    <StackPanel>
                                        <TextBlock Text="Recalage du solde de référence (€)" Foreground="{StaticResource TextMuted}" FontSize="12" FontWeight="Bold"/>
                                        <TextBox Text="{Binding NouveauSoldeReferenceEuros, UpdateSourceTrigger=PropertyChanged}" Background="{StaticResource BackgroundSecondary}" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderSubtle}" Padding="10" FontSize="14" Margin="0,8,0,12"/>
                                        <Button Content="Recaler le solde" Command="{Binding RecalerSoldeCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource GlassCard}">
                                    <StackPanel>
                                        <TextBlock Text="Exportation complète hors-ligne (§5.7)" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <TextBlock Text="Génère une archive ZIP (donnees.json + pièces jointes) sur le Bureau sans dépendance au serveur." Foreground="{StaticResource TextMuted}" FontSize="13" Margin="0,0,0,16"/>
                                        <Button Content="📦 Exporter mes données en ZIP" Command="{Binding ExporterZipCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left"/>
                                        <TextBlock Text="{Binding StatutSynchro}" Foreground="{StaticResource AccentSecondary}" FontSize="13" Margin="0,16,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </DataTemplate>

                    </ContentControl.Resources>
                </ContentControl>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
